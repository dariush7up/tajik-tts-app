# Streamlit Secrets Example
# Copy this to .streamlit/secrets.toml if you need to store secrets
# Note: For Streamlit Cloud, add secrets in the dashboard instead

# [secrets]
# API_KEY = "your-api-key-here"

# This app doesn't require secrets (no API keys needed)

